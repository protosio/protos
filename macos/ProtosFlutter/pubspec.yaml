name: protos_flutter
description: Flutter macOS client for the embedded Protos daemon.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  ffi: ^2.2.0
  fixnum: ^1.1.1
  flutter:
    sdk: flutter
  path: ^1.9.1
  protobuf: ^6.0.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

